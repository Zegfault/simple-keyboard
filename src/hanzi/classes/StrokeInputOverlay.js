class StrokeInputOverlay {
  constructor (top, right, bottom, left, xStrokes, yStrokes, zStrokes) {
    this.top = top
    this.right = right
    this.bottom = bottom
    this.left = left
    this.xStrokes = xStrokes
    this.yStrokes = yStrokes
    this.zStrokes = zStrokes
  }
}

export default StrokeInputOverlay
